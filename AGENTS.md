# 仓库协作入口

打开仓库本身不等于用户要求启用全局网页委托。用户要求部署时依次读取 README、文档/部署.md、配置/全局节点.md；维护源码时只按当前开发任务工作。

## 部署

- 核对当前来源、版本、宿主工具定义、运行时和既有用户配置；没有实际会话接口就报告缺口。
- 先说明准确目标路径及合并差异，在用户授权范围内操作；保留其他配置与已安装技能。
- 所有实际账号、会话和 Drive 位置由使用者提供或通过真实工具核对，不使用维护者的数据。
- 按配置模板中的边界标记合并；重复部署不得追加重复块。文件冲突先比较。
- 本地运行记录放使用者选择的位置，调用程序始终显式传入 `--db`。
- 网页执行参考可以直接从本仓库读取。安装到技能目录是可选项，先确认宿主位置、支持中文名称且不存在未知同名内容；不为安装而改写机器标识。
- 完成后报告安装文件、备份、能力检查、实际示例结果与未验证项，不把文字配置视作权限开通。

## 维护

首版只维护配置、文档、Python 会话池及 JavaScript 等待。不得提交本地配置、凭据、数据库、私人会话或业务图片。领域流程由独立原生技能决定。

程序修改运行对应已有测试，文档修改核对真实程序帮助、相对路径和部署结果。不要为了验证部署而覆盖使用者现用全局文件或数据库。
